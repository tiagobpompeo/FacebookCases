<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:circle="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:converters="clr-namespace:FacebookLogin.Converters" 
             x:Class="FacebookLogin.View.RegisterView">


     <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InvertBooleanConverter x:Key="InvertBooleanConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>

        <StackLayout>
            <Button CornerRadius="3"
                    x:Name="btnLogar"
                    Text="Entre com o Facebook"
                    BackgroundColor="#3b5998"
                    TextColor="White"
                    Command="{Binding FacebookLoginCommand}"/>
            <Image Source="{Binding FacebookUser.Picture}"
                   IsVisible="{Binding IsLogedInFacebook}" />
            <circle:CircleImage BorderThickness="2"
                                BorderColor="White"
                                Source="{Binding FacebookUser.Picture}"
                                IsVisible="{Binding IsLogedInFacebook}"
                                HeightRequest="150"
                                WidthRequest="150" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
